#include <bits/stdc++.h>
using namespace std;
int main(){
    char S;
    double total = 0.0;
    double N[12][12];
    for (int y=0;y<12;y++){
        for (int x=0;x<12;x++){
            cin >> N[y][x];
            if (x)
        }
    }
    return 0;
}